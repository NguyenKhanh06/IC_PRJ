import React from 'react';
import PropTypes from 'prop-types';
import Header from '../components/header/Header';

home.propTypes = {
    
};

function home(props) {
    return (
        <div>
            <Header/>
        </div>
    );
}

export default home;